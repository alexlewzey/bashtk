#!/bin/bash
path=$1
removestar -i $path
find $path -name "*.py" -exec pyupgrade --py311-plus {} \;
black $path
autoflake --remove-all-unused-imports --remove-unused-variables --in-place -r $path
isort $path
docformatter -r --in-place $path
